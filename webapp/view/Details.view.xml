<mvc:View
    controllerName="routingsplitapp.controller.Details"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
>
	<Page title="{i18n>detailsTitle}">
		<content>
            <HBox >
                <Text text="{Description}" />
            </HBox>
            <HBox>
                <Text text="{Status}" />
            </HBox>
        </content>
	</Page>

</mvc:View>